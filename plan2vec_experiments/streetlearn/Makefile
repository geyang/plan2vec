#run:
#	find . -name "*" | grep local_metric
#local-metric:
#	source activate plan2vec && python ./streetlearn_local_metric.py
# note:
#   the 1.5 cut-off gets to 99% accuracy.
#   the 1.05 cut-off gets to 95% accuracy.
#   we should consider adding a supervised metric to the local_metric.

