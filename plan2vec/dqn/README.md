## GMO on MDP

Currently running:

- `2D Navigation`
    - vanilla Q-learning: PointMass, dense reward
    - metric: Q as f(s, a), action space discrete, multi-headed Q function + Identity.
    - embedding-1: 
    - embedding-2: 
    - embedding-forward model
    - embedding-image: Not working, not active

Next:

- `InvertedPendulum`, `CartPole`. `2-link Arm`
